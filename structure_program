#include<stdio.h>
struct student
{
    // int rno;
    char name[10];
    int rno;
    
};
int main()
{
    struct student s[5];
    int n, sum =0;
    scanf("%d",&n);
      printf("students details:\n");
    for(int i=0;i<n;i++)
    {
     printf("enter the name of the student\n");
  scanf("%s",s[i].name);
     printf("enter the roll_no of the student\n");
  scanf("%d",&s[i].rno);   
    }
 
     for(int i=0;i<n;i++)
     {
  printf("name =%s\n",s[i].name);
    printf("roll_no=%d\n",s[i].rno);
     }
